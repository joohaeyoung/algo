#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;


struct Person {

	string name;
	int age;
	int sequence;

};

bool cmp(Person & a, Person& b) {

	if (a.age < b.age) {
		return true;
	}
	else if (a.age == b.age) {

	}

}

int main() {





}